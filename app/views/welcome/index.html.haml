.row
  - Article.last(4).each do |art|
    .col-12.col-md-6.col-lg-3.h-100
      .card.mb-3
        = link_to article_path(art) do
          = image_tag art.pictures.first ? art.pictures.first.image.url : 'placeholder.png', class: 'card-img-top'
        .card-body
          %h5.card-title= link_to art.title, article_path(art)
          .card-text= excerpt(art.text, '', radius: 50)
          .card-text
            %small.text-muted
              = t('welcome.article_created_caption', default: 'Utworzono:')
              = l(art.created_at.to_date)
